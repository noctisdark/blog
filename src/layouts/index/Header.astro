---
import Navigation from "@layouts/Navigation.astro";
import Footer from "@layouts/Footer.astro";
---

<header class="flex flex-col bg-base-300 w-full">
  <Navigation />
</header>

<style is:global>
  :root {
    --header-height: calc(var(--navbar-height));
  }

  header {
    z-index: 1;
    height: var(--header-height);
  }
</style>
